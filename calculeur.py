expressions = ('3','4','*','2','+')
operateurs = ('+', '-', '*')
resultats = []

def resultat(expressions):
    p = Stack()  # p est instance de Stack
    for expression in expressions:
        if operateurs
    

for e in expressions:
    resultats.append(resultat(e))
